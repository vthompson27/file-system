# Garante que o processador inicie em modo 32-bit.
arm_64bit=0

# Nome do arquivo do kernel a ser carregado.
kernel=kernel7.img

# Endereço de memória para o kernel.
kernel_address=0x8000

# Trava a frequência do core da VPU em 250MHz para estabilizar o clock da Mini UART.
core_freq=250

# Impede que o firmware passe tags de inicialização.
disable_commandline_tags=1